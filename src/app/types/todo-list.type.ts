import { todoListDetails } from "app/models/todo-list-details.model"

//beschreibt wie das json aussieht - das todolistmodel ist das tatsächliche obj.
export type TodoListType = {
    id :string,
    name:string,
    
 
}